declare module "*.css"

declare namespace JSX {
  interface IntrinsicElements {
    [elemName: string]: any
  }
}

export interface WindowConfig {
  title: string
  width: number
  height: number
  modal?: boolean
}

export declare class CSS {
  static load(path: string): void
}

export declare function morphState<S>(initialState: S | (() => S)): [S, (value: S | ((prev: S) => S)) => void];

export declare function morphEffect(effect: () => void | (() => void), deps?: readonly any[]): void;
