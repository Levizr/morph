// app.js — parsed as AST by Morph, compiled to C++
// JS runs at build time only — no runtime JS engine

// Example: respond to a custom event from C++
// document.querySelector("#root").on("ready", () => {
//   console.log("App ready")
// })
