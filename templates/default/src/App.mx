export default function App() {
  console.log("Morph app started!");
  return (
    <morph-window title="My App" width={800} height={600}>
      <div style={{
        backgroundColor: '#1a1a2e',
        padding: 32,
        display: 'flex',
        flexDirection: 'column',
        gap: 24
      }}>
        <h1 style={{ color: '#e8e8f0', fontSize: 32, fontWeight: 'bold' }}>
          {console.log("Rendered heading")}Morph Framework
        </h1>
        <p style={{ color: '#8888cc', fontSize: 14 }}>
          Edit src/App.mx to get started. Text automatically wraps when it exceeds container width, just like a browser.
        </p>

        <div style={{
          backgroundColor: '#16213e',
          padding: 24,
          borderRadius: 12,
          display: 'flex',
          flexDirection: 'column',
          overflow: 'auto',
          height: 160,
          gap: 12
        }}>
          <div style={{
            backgroundColor: '#0f3460',
            padding: '12px 16px',
            borderRadius: 8
          }}>
            <p style={{ color: '#e94560', fontSize: 14, fontWeight: 'bold' }}>
              Scrollable container
            </p>
            <p style={{ color: '#a0a0c0', fontSize: 12 }}>
              This container has overflow auto and a fixed height. Scroll with mouse wheel or drag the scrollbar.
            </p>
          </div>
          <div style={{
            backgroundColor: '#0f3460',
            padding: '12px 16px',
            borderRadius: 8
          }}>
            <p style={{ color: '#53d769', fontSize: 14, fontWeight: 'bold' }}>
              Item 2 border-radius + font-weight
            </p>
            <p style={{ color: '#a0a0c0', fontSize: 12 }}>
              Bold headings with normal weight body text, rounded corners on all cards.
            </p>
          </div>
          <div style={{
            backgroundColor: '#0f3460',
            padding: '12px 16px',
            borderRadius: 8
          }}>
            <p style={{ color: '#f1c40f', fontSize: 14, fontWeight: 'bold' }}>
              Item 3 console.log anywhere
            </p>
            <p style={{ color: '#a0a0c0', fontSize: 12 }}>
              Body, JSX expressions, and onClick handlers.
            </p>
          </div>
          <div style={{
            backgroundColor: '#0f3460',
            padding: '12px 16px',
            borderRadius: 8
          }}>
            <p style={{ color: '#9b59b6', fontSize: 14, fontWeight: 'bold' }}>
              Item 4 Runtime layout + resize
            </p>
            <p style={{ color: '#a0a0c0', fontSize: 12 }}>
              Window resize reflows everything. Text wraps to container width.
            </p>
          </div>
        </div>

        <button onClick={() => console.log("Button clicked!")}
                style={{ backgroundColor: '#3b82f6', color: '#fff',
                         padding: '12px 24px', borderRadius: 8, fontSize: 14,
                         fontWeight: 'bold' }}>
          Click me (console.log)
        </button>
      </div>
    </morph-window>
  )
}
